import BtnMainMenu from "../components/buttons/btn_main_menu";

const Homepage = () => {
  function handleOnClick() {}
  return (
    <div>
      <BtnMainMenu label="Kurse" onClick={handleOnClick} />
    </div>
  );
};

export default Homepage;
